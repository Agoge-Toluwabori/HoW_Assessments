import {NextResponse} from 'next/server';import {activityData} from '@/lib/activity-data';
export const dynamic='force-dynamic';export async function GET(){try{const d=await activityData(),show=!!d.settings.show_scores_on_activity;return NextResponse.json({settings:d.settings,attempts:d.attempts.map(x=>({...x,id:undefined,correctCount:show?x.correctCount:null,incorrectCount:null,unansweredCount:null,percentage:show?x.percentage:null,standing:show?x.standing:null}))},{headers:{'cache-control':'no-store'}})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
