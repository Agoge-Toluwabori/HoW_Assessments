'use client';import {useState} from 'react';
export default function Activity(){const [passcode,setPasscode]=useState(''),[minutes,setMinutes]=useState('30'),[isOpen,setOpen]=useState(true),[show,setShow]=useState(false),[message,setMessage]=useState('');async function save(){const r=await fetch('/api/admin/settings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({passcode,durationMinutes:minutes?Number(minutes):null,isOpen,showScores:show})});const j=await r.json();setMessage(r.ok?'Settings saved. New attempts will use this time limit.':j.error)}return <section className="card"><h2>Teacher controls</h2><p className="muted">The timer applies to new attempts. Clear the duration to make the assessment untimed.</p><label>Teacher passcode</label><input type="password" value={passcode} onChange={e=>setPasscode(e.target.value)}/><label>Test duration (minutes)</label><input type="number" min="1" max="480" value={minutes} onChange={e=>setMinutes(e.target.value)}/><label className="option"><input type="checkbox" checked={isOpen} onChange={e=>setOpen(e.target.checked)}/> Assessment open</label><label className="option"><input type="checkbox" checked={show} onChange={e=>setShow(e.target.checked)}/> Show scores on activity screen</label><button onClick={save}>Save settings</button>{message&&<p role="status">{message}</p>}</section>}
